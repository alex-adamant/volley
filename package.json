{
  "name": "volley",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run -ws build",
    "dev": "npm run -ws dev",
    "lint": "npm run -ws lint",
    "migrate:dev": "npm run -w @volley/db migrate:dev",
    "migrate:deploy": "npm run -w @volley/db migrate:deploy",
    "generate": "npm run -w @volley/db generate"
  },
  "devDependencies": {
    "@types/node": "^24.10.0"
  }
}
